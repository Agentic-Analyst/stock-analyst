# Stock Analyst Pipeline Requirements

# Core dependencies
PyYAML>=6.0.2
python-slugify>=8.0.4
newspaper3k>=0.2.8
google-search-results>=2.4.2
lxml_html_clean>=0.4.2

# AI/LLM functionality  
openai>=1.30.0
anthropic>=0.65.0
tiktoken>=0.9.0

# Financial data scraping
yfinance>=0.2.28
pandas>=2.0.0
numpy>=1.24.0

# Excel export
openpyxl>=3.1.0

# Data processing (usually included with Python but explicit for clarity)
# pathlib, json, csv, re, datetime, typing, dataclasses, collections - built-in modules
