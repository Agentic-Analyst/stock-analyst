Write the commentary for the Financial Model & Valuation section of a {company_name} report.

The tables below are already in the report, placed directly above your text. They are
emitted by code. Do NOT reproduce, reformat, summarise into a new table, or restate
them line by line — write prose that interprets them.

{tables}

**Your task** — 3 to 4 paragraphs, no headings, no tables:

1. One short paragraph on the DCF methodology as applied here (explicit forecast,
   terminal value by two methods, equity bridge).

2. The cost of capital. Walk through the build — risk-free rate, beta, equity risk
   premium, capital structure — rather than asserting the WACC is reasonable. State
   where the risk-free rate came from: the table's "Risk-free source" row says whether
   it is a live yield, a dated figure, or a proxy for a currency we have no sovereign
   feed for. If it is a proxy, say so plainly and say which direction that biases the
   valuation. If the beta source says the observed beta was floored or adjusted, say
   what that implies for confidence.

3. What the sensitivity grid shows. Using the grid's own numbers, compare the value
   change across a 50bp move in WACC against a full point of terminal growth, and say
   which input dominates. If the discount rate dominates, say so: the valuation is
   then largely a view on rates, not on the business.

4. The valuation range and the implied upside. Name every leg that enters the
   average — if a market-comps value is listed, it is one of them — and say which
   method is more appropriate here and why.

Every figure you quote must appear in the tables above. Do not introduce numbers that
are not there.
